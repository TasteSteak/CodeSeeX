CODESEEX_HOST=127.0.0.1
CODESEEX_PORT=8787
DEEPSEEK_BASE_URL=https://api.deepseek.com/
# Preferred credential source is Codex auth.json or an inbound Bearer header.
# This legacy env fallback is used only for direct upstream requests, not balance checks.
DEEPSEEK_API_KEY=
